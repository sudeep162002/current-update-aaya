import MotionHoc from "./MotionHoc";

const HomeComponent = () => {
  return( 
    <div class="Team">
    <h1>Home</h1>
  </div>
    )
};

const Home = MotionHoc(HomeComponent);

export default Home;
